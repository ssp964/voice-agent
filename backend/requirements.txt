# Speech-to-Text
faster-whisper==1.0.3

# Text-to-Speech (Coqui TTS)
TTS==0.22.0

# PyTorch CPU (macOS/Windows CPU)
torch==2.3.1
torchaudio==2.3.1

# Audio and numeric utils (pin numpy compatible with TTS on py3.9)
numpy==1.22.0
soundfile>=0.12

# API server
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
